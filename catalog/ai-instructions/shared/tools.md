# Tools: External Tool Integrations

Add tool integrations here as your project requires them.

## MCP Usage

MCPs (Model Context Protocol tools) are not invoked by default.

- Do not invoke an MCP unless the user explicitly requests it.
- Do not invoke an MCP unless the active pipeline definition explicitly requires it.
- When in doubt, do not invoke.
